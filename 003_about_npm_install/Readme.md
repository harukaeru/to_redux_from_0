```javascript
// Install this `lodash` (in this case) to use the library here location.
$ npm install lodash --save
$ node hello.js

// You can install in global to use at other directories
$ npm install webpack --g
```
